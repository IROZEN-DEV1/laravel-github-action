on: 
  push:
   branches:
      - master
      
name: deploy website on push
jobs:
  web-deploy:
     name: Deploy
     runs-on: ubuntu-latest
     steps:
     - name: Get latest code
       uses: actions/checkout@v2.3.2

     - name: Sync files
       uses: SamKirkland/FTP-Deploy-Action@v4.3.4
       with:
         server: ftp.netelmart.net
         username: dev2@dev2.netelmart.net
         password: ${{ secrets.FTP_PASSWORD_DEV2 }}
         server-dir: dev2.netelmart.net/